name: Run JS test suite (Local Test)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
    
    # Simple test to ensure workflow structure is valid
    - name: Echo test
      run: echo "Workflow structure test successful" 