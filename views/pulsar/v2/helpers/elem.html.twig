{#  ----------------------------------------------------------------------------

## Input

A standard input field, accepts a `type` attribute to switch between:

 * File
 * Password
 * Text (default)

### Example usage

	{{
		input.text({
			'name': 'MyAwesomeField'
		})
	}}

### Options

Option      | Type   | Description
----------- | ------ | ---------------------------------------------------------
class       | string | A space separated list of class names
data        | hash   | Data attributes by key/value
id          | string | A unique identifier, if required
form        | string | Specific one or more forms this label belongs to
name        | string | The name of this control
placeholder | string | A short hint that describes the expected value
type        | string | Defines the type of input to render (default text)
value       | string | Specifies the value of the input

#}
{% macro input(options) %}
{% spaceless %}
	{% import 'pulsar/v2/helpers/util.html.twig' as util %}

	<input
		{{ util.class('form__control', options) }}
		{{ util.checked(options.checked|default) }}
		{{ util.form(options.form|default) }}
		{{ util.id(options.id|default) }}
		{{ util.name(options.name|default) }}
		{{ util.placeholder(options.placeholder|default) }}
		{{ util.type(options.type|default('text')) }}
		{{ util.value(options.value|default) }}
		{{ util.data(options.data|default) }}
	/>

{% endspaceless %}
{% endmacro %}


{#  ----------------------------------------------------------------------------

## Label

Generates a `<label>` element to accompany an input

### Example usage

    {{
        form.label({
            'for': 'myInput'
        })
    }}

### Options

Option  | Type   | Description
------- | ------ | -------------------------------------------------------------
class   | string | A space separated list of class names
data    | hash   | Data attributes by key/value
for     | string | Specific which form element this label is bound to
form    | string | Specific one or more forms this label belongs to
id      | string | A unique identifier, if required
name    | string | The name of this control

#}
{% macro label(options) %}
{% spaceless %}
    {% import '@pulsar/pulsar/v2/helpers/util.html.twig' as util %}

    {% if options.label is not empty %}
	    <label
	        {{ util.class('control__label', options) }}
	        {{ util.for(options.for|default) }}
	        {{ util.form(options.form|default) }}
	        {{ util.id(options.id|default) }}
	        {{ util.data(options.data|default) }}
	    >
	        {{- options.label|raw -}}
	    </label>
	{% endif %}

{% endspaceless %}
{% endmacro %}


{#  ----------------------------------------------------------------------------

## Select

A standard select field

### Example usage

	{{
		input.text({
			'name': 'MyAwesomeField'
		})
	}}

### Options

Option      | Type   | Description
----------- | ------ | ---------------------------------------------------------
class       | string | A space separated list of class names
data        | hash   | Data attributes by key/value
form        | string | Specific one or more forms this label belongs to
id          | string | A unique identifier, if required
options     | hash   | Hash of <option> elements by { 'value': 'label' }
name        | string | The name of this control
placeholder | string | A short hint that describes the expected value
selected    | string | The `value` to have as the selected option
type        | string | Defines the type of input to render (default text)
value       | string | Specifies the value of the input

#}
{% macro select(options) %}
{% spaceless %}
	{% import 'pulsar/v2/helpers/util.html.twig' as util %}

	<select
		{{ util.class('form__control', options) }}
		{{ util.form(options.form|default) }}
		{{ util.id(options.id|default) }}
		{{ util.multiple(options.multiple|default) }}
		{{ util.name(options.name|default) }}
		{{ util.data(options.data|default) }}
	>

	{% for value, label in options.options %}
		<option
			{{ util.value(value) }}
			{% if value == options.selected %}
				selected
			{% endif %}
		>
			{{- label -}}
		</option>
	{% endfor %}

	</select>

{% endspaceless %}
{% endmacro %}


{#  ----------------------------------------------------------------------------

## Textarea

A standard texarea field.

### Example usage

	{{
		input.textarea({
			'name': 'MyAwesomeField'
		})
	}}

### Options

Option      | Type    | Description
----------- | ------- | --------------------------------------------------------
class       | string  | A space separated list of class names
data        | hash    | Data attributes by key/value
form        | string  | Specific one or more forms this label belongs to
id          | string  | A unique identifier, if required
name        | string  | The name of this control
placeholder | string  | A short hint that describes the expected value
rows        | integer | The height, in rows (default 2)
value       | string  | Specifies the value of the input

#}
{% macro textarea(options) %}
{% spaceless %}
	{% import 'pulsar/v2/helpers/util.html.twig' as util %}

	<textarea
		{{ util.class('form__control', options) }}
		{{ util.form(options.form|default) }}
		{{ util.id(options.id|default) }}
		{{ util.name(options.name|default) }}
		{{ util.placeholder(options.placeholder|default) }}
		{{ util.type(options.type|default) }}
		{{ util.value(options.value|default) }}
		{{ util.data(options.data|default) }}
		rows="{{ options.rows|default(2) }}"
	>
		{{- options.value -}}
	</textarea>

{% endspaceless %}
{% endmacro %}
