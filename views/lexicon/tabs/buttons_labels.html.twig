{% import 'helpers/html.html.twig' as html %}
{% import 'helpers/form.html.twig' as form %}
{% import 'helpers/flash.html.twig' as flash %}
<div class="tab__inner">
    <h2>Button variations</h2>

    <p>
        <!-- The normal type of button you should use -->
        {{ html.button(label = 'Default') }}

        <!-- In the context of a full UI, this is the main thing we want the user to do next -->
        {{ html.button(label = 'Primary', class = 'btn--primary') }}

        <!-- Indicates a successful or positive action -->
        {{ html.button(label = 'Success', class = 'btn--success') }}

        <!-- Indicates a dangerous or destructive action -->
        {{ html.button(label = 'Danger', class = 'btn--danger') }}

        <!-- Indicates caution should be taken here -->
        {{ html.button(label = 'Warning', class = 'btn--warning') }}

        <!-- Contextual button for informational alert messages -->
        {{ html.button(label = 'Info', class = 'btn--info') }}

        <!-- Rarely used, a good example is a 'locked content' button -->
        {{ html.button(label = 'Inverse', class = 'btn--inverse') }}

        {{ html.button(label = 'Disabled', class = 'is-disabled') }}
    </p>

    <h2>Button groups</h2>
    <p>
        <div class="btn__toolbar">
        {{
            html.button_group([ 
                html.button(label = 'Left'), 
                html.button(label = 'Middle'),
                html.button(label = 'Right') 
            ]) 
        }}
        {{
            html.button_group([ 
                html.button(label = html.icon('align-left')), 
                html.button(label = html.icon('align-center')),
                html.button(label = html.icon('align-right')), 
                html.button(label = html.icon('align-justify')) 
            ]) 
        }}
        </div>
    </p>

    <h2>Labels</h2>
    <p>
        {{ html.label(label = 'default') }}
        {{ html.label(label = 'primary', class = 'label--primary') }}
        {{ html.label(label = 'success', class = 'label--success') }}
        {{ html.label(label = 'warning', class = 'label--warning') }}
        {{ html.label(label = 'danger', class = 'label--danger') }}
        {{ html.label(label = 'info', class = 'label--info') }}
        {{ html.label(label = 'inverse', class = 'label--inverse') }}
    </p>

    <h2>Badges</h2>
    <p>
        {{ html.badge(label = '1') }}
        {{ html.badge(label = '2', class = 'badge--primary') }}
        {{ html.badge(label = '3', class = 'badge--success') }}
        {{ html.badge(label = '4', class = 'badge--warning') }}
        {{ html.badge(label = '5', class = 'badge--danger') }}
        {{ html.badge(label = '6', class = 'badge--info') }}
        {{ html.badge(label = '7', class = 'badge--inverse') }}
    </p>

    <h2>Dropdown/Dropup buttons</h2>
    <p>
        <div class="btn__toolbar">
        {% 
            set example_dropdown = [
                html.link(label = html.icon('save') ~ ' Save', href = '#'),
                html.link(label = html.icon('print') ~ ' Print'),
                html.link(label = html.icon('lock') ~ ' Lock'),
                html.link(label = html.icon('group') ~ ' Collaborators ' ~ html.badge(3)),
                html.divider(),
                html.link(label = html.icon('trash') ~ ' Delete', href = '#', class = 'link--danger')
            ]
        %}
        {{ html.button_dropdown(label = 'Drop Down', menu_items = example_dropdown) }}
        {{ html.button_dropup(label = 'Drop Up', menu_items = example_dropdown) }}
        </div>
    </p>


    <h2>Icons, Badges and Buttons (oh my!)</h2>
    <p>
        <div class="btn__toolbar">
            {{ html.button_dropdown(label = html.icon('youtube-play') ~ ' Default ' ~ html.badge('99')) }}
            {{ html.button(label = html.icon('inbox') ~ ' Primary ' ~ html.badge('99'), class = 'btn--primary') }}
            {{ html.button(label = html.icon('ok') ~ ' Success ' ~ html.badge('99'), class = 'btn--success') }}
            {{ html.button(label = html.icon('warning-sign') ~ ' Warning ' ~ html.badge('99'), class = 'btn--warning') }}
            {{ html.button(label = html.icon('trash') ~ ' Danger ' ~ html.badge('99'), class = 'btn--danger') }}
            {{ html.button(label = html.icon('info-sign') ~ ' Info ' ~ html.badge('99'), class = 'btn--info') }}
            {{ html.button(label = html.icon('lock') ~ ' Inverse ' ~ html.badge('99'), class = 'btn--inverse') }}
        </div>
    </p>

    <h2>All the things!</h2>
    <p>
    {{ html.button_dropdown(label = html.icon('folder-close') ~ ' Busy Button ' ~ html.badge('99', 'badge--primary'), menu_items = example_dropdown, open = true) }}
    </p>

    <p class="columns columns--2">Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nam et enim sed lorem sollicitudin tristique sed at erat. Sed non nunc at nibh vestibulum dictum. Vestibulum a blandit tellus. In quis felis rhoncus mi imperdiet gravida eget quis augue. Praesent rutrum fringilla nisi, sit amet commodo purus pellentesque eu. Curabitur blandit arcu at rutrum ullamcorper. Maecenas vitae faucibus nunc. Phasellus eu quam aliquet, malesuada odio ut, dapibus lorem. Nulla nec urna rutrum, consequat est sit amet, consequat mauris. Maecenas vel mauris id urna viverra ullamcorper. Etiam nisi nibh, pulvinar vel elementum luctus, dapibus sit amet leo. Lorem ipsum dolor sit amet, consectetur adipiscing elit. Nam et enim sed lorem sollicitudin tristique sed at erat. Sed non nunc at nibh vestibulum dictum. Vestibulum a blandit tellus. In quis felis rhoncus mi imperdiet gravida eget quis augue. Praesent rutrum fringilla nisi, sit amet commodo purus pellentesque eu. Curabitur blandit arcu at rutrum ullamcorper. Maecenas vitae faucibus nunc. Phasellus eu quam aliquet, malesuada odio ut, dapibus lorem. Nulla nec urna rutrum, consequat est sit amet, consequat mauris. Maecenas vel mauris id urna viverra ullamcorper. Etiam nisi nibh, pulvinar vel elementum luctus, dapibus sit amet leo.</p>
</div>