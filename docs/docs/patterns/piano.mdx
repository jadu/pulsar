---
id: piano
title: Piano
sidebar_label: Piano
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

The piano pattern allows you to switch between different panels of content within the same UI. Commonly used in integration and messages UIs.

![Screenshot showing piano example](/img/piano.png)

## Dependencies

You will need to import the `_component.piano.scss` sass partial in your main sass file.</p>

```scss title="pulsar.scss"
@import '/path/to/pulsar/stylesheets/component.piano';
```