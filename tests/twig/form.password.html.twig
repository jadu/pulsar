{% import '@pulsar/pulsar/v2/helpers/form.html.twig' as form %}

<h1>form.password</h1>

<pre>
form.password({
	'label': 'Label'
})
</pre>

{%
	set output = form.password({
		'label': 'Label'
	})
 %}

{{ output }}

<pre>{{ output|e }}</pre>
