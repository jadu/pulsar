{% import '@pulsar/pulsar/v2/helpers/form.html.twig' as form %}

<h1>form.checkbox_inline</h1>

<pre>
form.checkbox_inline({
	'label': 'Label'
})
</pre>

{%
	set output = form.checkbox_inline({
		'label': 'Label'
	})
 %}

{{ output }}

<pre>{{ output|e }}</pre>
