{% import '@pulsar/pulsar/v2/helpers/form.html.twig' as form %}

<h1>form.select2</h1>

<pre>
form.select2({
	'label': 'Label',
	'options': {
		'foo': 'Foo',
		'bar': 'Bar'
	}
})
</pre>

{%
	set output = form.select2({
		'label': 'Label',
		'options': {
			'foo': 'Foo',
			'bar': 'Bar'
		}
	})
 %}

{{ output }}

<pre>{{ output|e }}</pre>
