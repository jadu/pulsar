{% import '@pulsar/pulsar/v2/helpers/form.html.twig' as form %}

<h1>form.radio_inline</h1>

<pre>
form.radio_inline({
	'label': 'Label'
})
</pre>

{%
	set output = form.radio_inline({
		'label': 'Label'
	})
 %}

{{ output }}

<pre>{{ output|e }}</pre>
