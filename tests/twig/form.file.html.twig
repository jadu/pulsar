{% import '@pulsar/pulsar/v2/helpers/form.html.twig' as form %}

<h1>form.file</h1>

<pre>
form.file({
	'label': 'Label'
})
</pre>

{%
	set output = form.file({
		'label': 'Label'
	})
 %}

{{ output }}

<pre>{{ output|e }}</pre>
