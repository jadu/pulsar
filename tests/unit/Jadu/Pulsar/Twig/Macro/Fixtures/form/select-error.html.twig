{#
    Test basic select input
#}
{% import '@pulsar/pulsar/v2/helpers/form.html.twig' as form %}
{{
    form.select({
        'error': 'my error',
        'options': {
            'foo': 'bar',
            'bar': 'baz'
        }
    })
}}
