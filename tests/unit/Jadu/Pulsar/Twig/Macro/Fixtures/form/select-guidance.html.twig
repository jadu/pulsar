{#
    Test basic select input with guidance
#}
{% import '@pulsar/pulsar/v2/helpers/form.html.twig' as form %}
{{
    form.select({
        'label': 'foo',
        'guidance': 'bar',
        'options': {
            'baz': 'baz',
            'qux': 'qux'
        }
    })
}}
