{#
    Test toggle switch with label
#}
{% import '@pulsar/pulsar/v2/helpers/form.html.twig' as form %}

{{
    form.toggle_switch({
        'label': 'Toggle',
        'guidance': 'bar',
        'id': 'toggletest'
    })
}}
