{#
    Test bare select input
#}
{% import '@pulsar/pulsar/v2/helpers/form.html.twig' as form %}
{{
    form.select({
        'bare': true,
        'options': {
            'foo': 'bar',
            'bar': 'baz'
        }
    })
}}
