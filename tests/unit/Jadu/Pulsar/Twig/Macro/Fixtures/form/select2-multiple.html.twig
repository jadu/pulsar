{#
    Test multiple choice select2 input
#}
{% import '@pulsar/pulsar/v2/helpers/form.html.twig' as form %}
{{
    form.select2({
        'multiple': true,
        'options': {
            'foo': 'bar',
            'bar': 'baz'
        }
    })
}}
