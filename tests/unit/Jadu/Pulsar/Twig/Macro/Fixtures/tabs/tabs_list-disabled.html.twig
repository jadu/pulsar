{% import '@pulsar/pulsar/v2/helpers/tabs.html.twig' as tabs %}
{%
    set nav = [
        {
          "id"    : "foo",
          "label" : "bar",
          "src"   : 'baz',
          "disabled": true
        }
    ]
%}
{{ tabs.list(nav) }}
{{ tabs.content(nav) }}
