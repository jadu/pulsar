{% extends '@cssTests/visual-regression-layout.html.twig' %}
{% block body %}
{%
    set nav = [
        {
          "id"    : "foo",
          "label" : "bar",
          "src"   : 'baz',
          "disabled": true
        }
    ]
%}
{{ tabs.list(nav) }}
{{ tabs.content(nav) }}
{% endblock %}
