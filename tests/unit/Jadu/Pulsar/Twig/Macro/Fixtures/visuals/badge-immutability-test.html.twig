{% extends '@cssTests/visual-regression-layout.html.twig' %}
{% block body %}
<h3>Normal</h3>
text {{
    html.badge({
        label: '1'
    })
}}
{{
    html.badge({
        label: '2', class: 'badge--primary'
    })
}}
{{
    html.badge({
        label: '3', class: 'badge--success'
    })
}}
{{
    html.badge({
        label: '4', class: 'badge--warning'
    })
}}
{{
    html.badge({
        label: '5', class: 'badge--danger'
    })
}}
{{
    html.badge({
        label: '6', class: 'badge--info'
    })
}}
{{
    html.badge({
        label: '7', class: 'badge--inverse'
    })
}}
<hr />
<h3>Test ignore muted</h3>

<p class="muted">
text {{
    html.badge({
        label: '1'
    })
}}
{{
    html.badge({
        label: '2', class: 'badge--primary'
    })
}}
{{
    html.badge({
        label: '3', class: 'badge--success'
    })
}}
{{
    html.badge({
        label: '4', class: 'badge--warning'
    })
}}
{{
    html.badge({
        label: '5', class: 'badge--danger'
    })
}}
{{
    html.badge({
        label: '6', class: 'badge--info'
    })
}}
{{
    html.badge({
        label: '7', class: 'badge--inverse'
    })
}}
</p>

<hr>
<h3>Test ignore inherited test colour</h3>

<p style="color: red;">
text {{
    html.badge({
        label: '1'
    })
}}
{{
    html.badge({
        label: '2', class: 'badge--primary'
    })
}}
{{
    html.badge({
        label: '3', class: 'badge--success'
    })
}}
{{
    html.badge({
        label: '4', class: 'badge--warning'
    })
}}
{{
    html.badge({
        label: '5', class: 'badge--danger'
    })
}}
{{
    html.badge({
        label: '6', class: 'badge--info'
    })
}}
{{
    html.badge({
        label: '7', class: 'badge--inverse'
    })
}}
</p>

<hr />
<h3>Test badges inside buttons</h3>
<div class="btn__toolbar">
    text {{
        html.button({
            'label': html.badge({ 'label': '123' })
        })
    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'btn--primary'
        })

    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'btn--success'
        })

    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'btn--warning'
        })

    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'btn--danger'
        })

    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'btn--info'
        })

    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'btn--inverse'
        })

    }}
</div>

<hr />
<h3>Test badges inside disabled buttons</h3>
<div class="btn__toolbar">
    text {{
        html.button({
            'label': html.badge({ 'label': '123' }),
            'class': 'is-disabled'
        })
    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'is-disabled btn--primary'
        })

    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'is-disabled btn--success'
        })

    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'is-disabled btn--warning'
        })

    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'is-disabled btn--danger'
        })

    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'is-disabled btn--info'
        })

    }}
    {{
        html.button({
            label: html.badge({ 'label': '123' }),
            class: 'is-disabled btn--inverse'
        })

    }}
</div>
{% endblock %}
