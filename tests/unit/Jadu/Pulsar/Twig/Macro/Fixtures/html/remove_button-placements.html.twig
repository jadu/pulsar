{#
    Test remove button placements
#}
{% import '@pulsar/pulsar/v2/helpers/html.html.twig' as html %}
{{
    html.remove_button({
        'placement': 'top'
    })
}}

{{
    html.remove_button({
        'placement': 'right'
    })
}}

{{
    html.remove_button({
        'placement': 'bottom'
    })
}}

{{
    html.remove_button({
        'placement': 'left'
    })
}}
