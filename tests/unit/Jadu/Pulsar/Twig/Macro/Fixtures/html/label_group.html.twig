{#
    Test label group
#}
{% import '@pulsar/pulsar/v2/helpers/html.html.twig' as html %}
{{
    html.label_group({
        'labels': [
            html.label({ 'label': 'foo' }),
            html.label({ 'label': 'bar' })
        ]
    })
}}
