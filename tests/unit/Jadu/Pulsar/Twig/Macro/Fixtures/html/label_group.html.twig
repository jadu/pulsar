{% extends '@cssTests/visual-regression-layout.html.twig' %}
{% block body %}
{{
    html.label_group({
        'labels': [
            html.label({ 'label': 'foo' }),
            html.label({ 'label': 'bar' })
        ]
    })
}}
{% endblock %}
